import { StyleSheet } from 'aphrodite';

export default StyleSheet.create({
    main: {
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        flexGrow: '1',
    }
})
