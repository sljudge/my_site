import { StyleSheet } from 'aphrodite'

export default StyleSheet.create({
    loadingContainer: {
        display: 'flex',
        justifyContent: "center",
        alignItems: "center",
        borderRadius: '50%',
        height: '9rem',
        width: '9rem'
    },
    loading: {
        minHeight: '5rem',
        minWidth: '5rem',
        marginleft: 'auto'
    }
})